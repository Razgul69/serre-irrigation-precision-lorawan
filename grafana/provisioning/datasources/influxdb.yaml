apiVersion: 1

datasources:
  - name: InfluxDB-Sondes
    type: influxdb
    access: proxy
    url: http://localhost:8086
    jsonData:
      version: Flux
      organization: ird
      defaultBucket: sondes
      tlsSkipVerify: true
    secureJsonData:
      token: ${INFLUX_TOKEN}   # remplacer par votre token InfluxDB
    isDefault: true
    editable: true

